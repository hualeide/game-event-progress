services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      HOST: "0.0.0.0"
      PORT: "8080"
      UPDATE_INTERVAL_HOURS: "6"
    volumes:
      # 持久化抓取结果与日志（可选）
      - ./data:/app/data
      - ./public/data:/app/public/data
    restart: unless-stopped
